"use client";

import { PageShell } from "@/components/page-shell";

export default function OCPMConformancePage() {
  return (
    <PageShell
      title="OCPM Conformance"
      description="Object-centric conformance checking will live here once the workflow and diagnostics are ready."
    >
      <section className="panel">
        <div className="empty-panel">OCPM conformance is currently in development.</div>
      </section>
    </PageShell>
  );
}
